_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
Gemfile.lock
.DS_Store
*.swp
*~

# PRIVATE — never push to the public Pages repo.
# Resumes, accomplishments, brain dump, job-search strategy (comp bands,
# target companies, honesty corrections). Kept local only.
source/
CLAUDE.md
# Unpublished drafts stay local until reviewed and moved into _posts/.
_drafts/

# Local-only Claude Code project commands/settings.
.claude/
# Local-only design mockup (4-variant theme picker). Not for the live site.
_design-preview.html
# Local-only static preview page for the chatbot widget.
_local-preview.html
# Local-only source HTML used to render the downloadable resume PDF.
resume-pdf-source.html
