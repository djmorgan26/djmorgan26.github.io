node_modules/
.wrangler/
package-lock.json
*.log

# Local secrets — never commit. .dev.vars is what `wrangler dev` reads.
.dev.vars
.dev.vars.*
.env
.env.*

# Eval output snapshot (regenerated by `npm run eval`); not source.
test/baseline.json
