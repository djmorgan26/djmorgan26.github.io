{
  "name": "ask-david-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker powering the 'Ask David' chatbot on djmorgan26.github.io",
  "scripts": {
    "dev": "wrangler dev",
    "build:context": "node scripts/build-context.mjs",
    "predeploy": "node scripts/build-context.mjs",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test test/*.test.mjs",
    "eval": "node test/eval.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
